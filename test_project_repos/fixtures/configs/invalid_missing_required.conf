# Invalid Configuration - Missing Required Fields
# This configuration is missing critical required parameters

# Missing: GITHUB_ORGANIZATION (required)
# Missing: ASSIGNMENT_NAME (required)
# Missing: STUDENTS_FILE (required)

# Optional fields present
OUTPUT_DIR=./output
VERBOSE=true
DRY_RUN=false

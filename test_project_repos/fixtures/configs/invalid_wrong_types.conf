# Invalid Configuration - Wrong Data Types
# Contains values with incorrect types (string instead of boolean/number, etc.)

GITHUB_ORGANIZATION=test-org
ASSIGNMENT_NAME=test-assignment
STUDENTS_FILE=students.txt

# Boolean fields with wrong values
DRY_RUN=maybe
VERBOSE=yes
FORCE=1
PRIVATE_REPOS=enabled
AUTO_MERGE=on

# Numeric fields with wrong values
PARALLEL_LIMIT=many
TIMEOUT=forever
CLONE_DEPTH=all

# Enum-like fields with invalid values
DEFAULT_BRANCH=
COLLABORATOR_PERMISSION=superuser

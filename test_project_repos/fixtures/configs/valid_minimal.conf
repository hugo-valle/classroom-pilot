# Valid Minimal Configuration
# Contains only required fields for classroom-pilot operations

# Required: GitHub organization name (must match CLI schema)
GITHUB_ORGANIZATION=test-org

# Required: Assignment name/prefix
ASSIGNMENT_NAME=test-assignment

# Required: Student list file
STUDENTS_FILE=students.txt

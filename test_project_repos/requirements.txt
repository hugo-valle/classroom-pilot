# Requirements for test environment setup
# Core package management
poetry>=1.6.0

# Essential testing dependencies
pytest>=7.0.0
pytest-mock>=3.10.0

# Core project dependencies (minimum needed for CLI to work)
typer>=0.12.0
click>=8.0.0,<8.2.0
pyyaml>=6.0.1
requests>=2.31.0

# Additional utilities that might be needed during testing
setuptools>=65.0.0
wheel>=0.38.0